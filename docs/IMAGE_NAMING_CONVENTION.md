# 图片命名规范文档

## 📸 命名规则

### 格式说明
所有房源和案例图片必须遵循以下命名格式：

```
{category}-{number}-{sequence}.{extension}
```

### 格式组成
1. **category**（分类前缀）：3位小写字母
   - `biz` - 生意转让 (Business For Sale)
   - `ind` - 工业仓库 (Industrial Warehouse)
   - `ret` - 办公商铺 (Office/Retail Space)

2. **number**（房源编号）：2位数字，从 01 开始递增
   - 示例：`01`, `02`, `22`, `99`

3. **sequence**（图片序号）：1位数字，从 1 开始递增
   - 示例：`1`, `2`, `3`, `4`

4. **extension**（文件扩展名）：小写
   - `.jpg` 或 `.png`

### 完整示例
- ✅ `biz-22-1.jpg` - 生意转让房源编号 22 的第 1 张图片
- ✅ `biz-22-2.jpg` - 生意转让房源编号 22 的第 2 张图片
- ✅ `ind-01-1.jpg` - 工业仓库房源编号 01 的第 1 张图片
- ✅ `ret-50-3.jpg` - 办公商铺房源编号 50 的第 3 张图片

## 📂 目录结构

```
client/public/assets/images/listings/
├── business/          # 生意转让
│   ├── biz-22-1.jpg
│   ├── biz-22-2.jpg
│   ├── biz-22-3.jpg
│   └── biz-22-4.jpg
├── industrial/        # 工业仓库
│   ├── ind-01-1.jpg
│   ├── ind-01-2.jpg
│   └── ind-02-1.jpg
└── retail/           # 办公商铺
    ├── ret-01-1.jpg
    ├── ret-01-2.jpg
    └── ret-02-1.jpg
```

## 📝 JSON 引用格式

在 JSON 数据文件中，图片路径应使用完整的相对路径：

```json
{
  "id": "biz-22",
  "images": [
    "/assets/images/listings/business/biz-22-1.jpg",
    "/assets/images/listings/business/biz-22-2.jpg",
    "/assets/images/listings/business/biz-22-3.jpg",
    "/assets/images/listings/business/biz-22-4.jpg"
  ]
}
```

**注意：**
- JSON 中的 `id` 字段必须与图片编号一致（不含序列号）
- 图片路径必须以 `/assets/images/listings/` 开头
- 所有文件名必须小写（系统区分大小写）

## 🔄 上传流程

### 标准流程
1. **准备图片**：将图片文件命名为规范格式
   - 示例：`biz-24-1.jpg`, `biz-24-2.jpg`

2. **放置文件**：将图片放入对应分类目录
   - 生意转让：`client/public/assets/images/listings/business/`
   - 工业仓库：`client/public/assets/images/listings/industrial/`
   - 办公商铺：`client/public/assets/images/listings/retail/`

3. **更新 JSON**：在对应的 JSON 文件中添加房源数据
   - 生意转让：`client/public/data/business.json`
   - 工业仓库：`client/public/data/industrial.json`
   - 办公商铺：`client/public/data/retail.json`

4. **验证路径**：确保 JSON 中的图片路径与实际文件名 100% 一致
5. **提交代码**：提交到 GitHub，触发 Vercel 自动部署

### 自动重命名脚本（可选）

可以使用以下 Shell 脚本批量重命名：

```bash
# 示例：重命名生意转让图片
mv "2026-01-10 16_25_53-NVIDIA GeForce Overlay.jpg" "biz-023-1.jpg"
mv "2026-01-10 16_25_59-NVIDIA GeForce Overlay.jpg" "biz-023-2.jpg"
```

## ⚠️ 注意事项

1. **大小写敏感**：
   - ❌ 错误：`Biz-023-1.jpg`
   - ✅ 正确：`biz-023-1.jpg`

2. **序列号递增**：
   - 每个房源的图片从 `1` 开始递增
   - 单个房源最多支持 9 张图片（序列号 1-9）

3. **编号唯一性**：
   - 同一分类下的房源编号必须唯一
   - 建议使用 Excel 或 Notion 记录已使用的编号

4. **文件格式**：
   - 优先使用 `.jpg` 格式（兼容性好）
   - 使用 `.png` 时需考虑文件大小

## 📊 编号管理表

建议使用以下表格记录已使用的编号：

| 分类 | 编号 | 房源标题 | 上传日期 | 图片数量 |
|------|------|----------|----------|----------|
| biz | 023 | 露营地餐厅转让 | 2026-01-31 | 4 |
| biz | 024 | 咖啡店转让 | 2026-02-01 | 3 |
| ind | 001 | Ontario 高仓 | 2026-01-30 | 2 |
| ret | 001 | 罗兰岗商铺 | 2026-01-29 | 2 |

## 🚀 优势

1. **易于管理**：统一命名格式，便于查找和组织
2. **避免冲突**：编号唯一性避免文件名重复
3. **自动匹配**：JSON 与文件名自动关联，减少错误
4. **可扩展性**：支持无限数量的房源和图片

## 📝 更新日志

- **2026-01-31**：创建命名规范，首个房源编号 `biz-023` 露营地餐厅
